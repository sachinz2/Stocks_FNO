# Web framework
fastapi
uvicorn[standard]

# Database
sqlalchemy[asyncio]
alembic
aiomysql
mysql-connector-python
cryptography

# Redis
redis[asyncio]

# Data validation
pydantic
pydantic-settings

# HTTP client (Telegram, external APIs)
httpx

# Scheduler
apscheduler

# Timezone support
pytz

# Dashboard
streamlit

# WebSocket
websockets

# Auth
PyJWT

# Data processing
pandas
numpy

# Dynamic NSE holiday calendar — replaces hardcoded date list in src/core/utils.py.
# Bundles accurate exchange calendars for 50+ markets; XNSE covers all NSE holidays.
# Run `pip install --upgrade exchange-calendars` once a year to pull the latest data.
exchange-calendars

# Retry logic
tenacity

# Zerodha broker — v4.x uses websocket-client; v5.x switched to autobahn which
# fails the WebSocket upgrade with Zerodha's server (403 Forbidden)
kiteconnect==4.2.0
pyotp
# Headless browser for Zerodha Connect authorize step (React SPA — needs real browser)
playwright

# ML
xgboost
lightgbm
scikit-learn
joblib

# Testing
pytest
pytest-asyncio
